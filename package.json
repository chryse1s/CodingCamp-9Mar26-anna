{
  "name": "productivity-dashboard",
  "version": "1.0.0",
  "description": "A standalone web application for managing time and tasks",
  "type": "module",
  "scripts": {
    "test": "vitest --run",
    "test:watch": "vitest",
    "test:coverage": "vitest --run --coverage",
    "test:ui": "vitest --ui"
  },
  "keywords": ["productivity", "dashboard", "timer", "tasks"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@vitest/ui": "^1.0.0",
    "fast-check": "^3.15.0",
    "jsdom": "^23.0.0",
    "vitest": "^1.0.0"
  }
}
